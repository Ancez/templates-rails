source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in templates-rails.gemspec.
gemspec

gem 'pg'
gem 'puma'
gem 'sprockets-rails'
gem 'rspec-rails'
gem 'capybara'
gem 'byebug'

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"
